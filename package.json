{
  "name": "ggi-astro-init",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npx esbuild worker-wrapper.js --bundle --format=esm --outfile=dist/client/_worker.js --conditions=workerd --platform=neutral '--external:node:*' '--external:cloudflare:*' && npx pagefind --site dist/client --root-selector main",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.7.0",
    "@astrojs/sitemap": "^3.7.2",
    "@portabletext/to-html": "^5.0.2",
    "@sanity/astro": "^3.3.1",
    "@sanity/client": "^7.22.0",
    "astro": "^6.1.9",
    "pagefind": "^1.5.2",
    "wrangler": "^4.104.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
